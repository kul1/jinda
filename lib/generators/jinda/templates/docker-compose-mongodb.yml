version: '3.5'
services:
  mongodb:
    image: mongo:latest
    container_name: jinda_mongodb
    ports:
      - "27888:27017"
    volumes:
      - mongodb_data:/data/db
    environment:
      - MONGO_INITDB_DATABASE=jinda_development

volumes:
  mongodb_data:
