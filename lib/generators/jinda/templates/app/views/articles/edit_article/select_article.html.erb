&lt;%
  if current_ma_user.role.upcase.split(&#39;,&#39;).include?(&quot;A&quot;)
    article = Article.all.desc(:created_at)
  else
    article = Article.where(user: current_ma_user).desc(:created_at)
  end
%&gt;
&lt;div class=&quot;mb-3&quot;&gt;
  &lt;%= label_tag :title, &#39;Article title&#39;, class: &quot;form-label&quot; %&gt;
  &lt;%= select_tag :title, options_from_collection_for_select(article, :id, :title), class: &quot;form-select&quot;, &quot;data-native-menu&quot;=&gt;&quot;false&quot; %&gt;
&lt;/div&gt;