&lt;%
  if current_ma_user.role.upcase.split(&#39;,&#39;).include?(&quot;A&quot;)
    note= Note.all.desc(:created_at)
  else
    note= Note.where(user: current_ma_user).desc(:created_at)
  end
%&gt;
&lt;div class=&quot;mb-3&quot;&gt;
  &lt;%= label_tag :title, &#39;Select Note&#39;, class: &quot;form-label&quot; %&gt;
  &lt;%= select_tag :id, options_from_collection_for_select(note, :id, :title), class: &quot;form-select&quot;, &quot;notee-menu&quot;=&gt;&quot;false&quot; %&gt;
&lt;/div&gt;