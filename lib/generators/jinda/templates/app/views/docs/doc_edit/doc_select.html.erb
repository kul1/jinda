&lt;%
  if current_ma_user.role.upcase.split(&#39;,&#39;).include?(&quot;A&quot;)
    note= Jinda::Doc.all.desc(:created_at)
  else
    note= Jinda::Doc.where(user: current_ma_user).desc(:created_at)
  end
%&gt;
&lt;div class=&quot;mb-3&quot;&gt;
  &lt;%= label_tag :filename, &#39;Select Document&#39;, class: &quot;form-label&quot; %&gt;
  &lt;%= select_tag :id, options_from_collection_for_select(note, :id, :filename), class: &quot;form-select&quot;, &quot;notee-menu&quot;=&gt;&quot;false&quot; %&gt;
&lt;/div&gt;